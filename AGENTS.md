# Workflow

- After completing changes in this repository, run relevant checks, commit the
  task's changes, and push the current branch to `origin` unless the user says
  otherwise. Report the commit and push result.
- Keep commits limited to this custom node repository and the current task.
